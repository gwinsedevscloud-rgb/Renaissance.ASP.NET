<td class="text-end module-table-actions @CssClass">
    <div class="client-actions">
        <a class="module-row-action" href="@Href" title="@Title" aria-label="@Title">
            <i class="bi @Icon"></i>
            @if (!string.IsNullOrWhiteSpace(Label))
            {
                <span class="module-row-action-label">@Label</span>
            }
        </a>
    </div>
</td>

@code {
    [Parameter, EditorRequired] public string Href { get; set; } = string.Empty;
    [Parameter] public string Title { get; set; } = "Open";
    [Parameter] public string Icon { get; set; } = "bi-pencil";
    [Parameter] public string? Label { get; set; }
    [Parameter] public string? CssClass { get; set; }
}
